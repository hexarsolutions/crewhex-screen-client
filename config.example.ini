[settings]
# CrewHex Screen Client configuration
# api_base      - CrewHex API the screen talks to
# display_name  - pre-fill shown to the tenant when pairing
# port          - local kiosk server port (Chromium points here)
api_base = https://api.crewhex.com
display_name = Screen
port = 8080
